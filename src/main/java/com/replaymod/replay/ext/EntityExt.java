package com.replaymod.replay.ext;

public interface EntityExt {
   float replaymod$getTrackedYaw();

   void replaymod$setTrackedYaw(float var1);

   float replaymod$getTrackedPitch();

   void replaymod$setTrackedPitch(float var1);
}
