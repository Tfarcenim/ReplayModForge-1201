package com.replaymod.lib.de.johni0702.minecraft.gui.versions.callbacks;

import com.replaymod.lib.de.johni0702.minecraft.gui.utils.Event;
import java.util.Iterator;

public interface
KeyboardCallback {/*
   Event<KeyboardCallback> EVENT = Event.create((listeners) -> {
      return new KeyboardCallback() {
         public boolean keyPressed(int keyCode, int scanCode, int modifiers) {
            Iterator var4 = listeners.iterator();

            KeyboardCallback listener;
            do {
               if (!var4.hasNext()) {
                  return false;
               }

               listener = (KeyboardCallback)var4.next();
            } while(!listener.keyPressed(keyCode, scanCode, modifiers));

            return true;
         }

         public boolean keyReleased(int keyCode, int scanCode, int modifiers) {
            Iterator var4 = listeners.iterator();

            KeyboardCallback listener;
            do {
               if (!var4.hasNext()) {
                  return false;
               }

               listener = (KeyboardCallback)var4.next();
            } while(!listener.keyReleased(keyCode, scanCode, modifiers));

            return true;
         }

         public boolean charTyped(char charCode, int scanCode) {
            Iterator var3 = listeners.iterator();

            KeyboardCallback listener;
            do {
               if (!var3.hasNext()) {
                  return false;
               }

               listener = (KeyboardCallback)var3.next();
            } while(!listener.charTyped(charCode, scanCode));

            return true;
         }
      };
   });

   boolean keyPressed(int var1, int var2, int var3);

   boolean keyReleased(int var1, int var2, int var3);

   boolean charTyped(char var1, int var2);*/
}
